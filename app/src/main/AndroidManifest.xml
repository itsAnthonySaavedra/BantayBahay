<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Location Permissions for GPS -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" /> <!-- Internet Permission (for Firebase) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BantayBahay">
        <activity
            android:name=".ChangePassword.ChangePasswordActivity"
            android:exported="false" />
        <!-- Splash Screen - LAUNCHER (First screen) -->
        <activity
            android:name=".SplashScreen.SplashScreenActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Profile Activity -->
        <activity
            android:name=".Profile.ProfileActivity"
            android:exported="false" /> <!-- Forgot Password Activity -->
        <activity
            android:name=".ForgotPassword.ForgotPasswordActivity"
            android:exported="false" /> <!-- Onboarding Activities -->
        <activity
            android:name=".Onboarding.Onboarding3Activity"
            android:exported="false" />
        <activity
            android:name=".Onboarding.Onboarding2Activity"
            android:exported="false" />
        <activity
            android:name=".Onboarding.Onboarding1Activity"
            android:exported="false" /> <!-- Settings Activity -->
        <activity
            android:name=".Settings.SettingsActivity"
            android:exported="false" /> <!-- Details Activity -->
        <activity
            android:name=".Detail.DetailsActivity"
            android:exported="false" /> <!-- Dashboard Activity -->
        <activity
            android:name=".Dashboard.DashboardActivity"
            android:exported="false" /> <!-- Register Activity -->
        <activity
            android:name=".Register.RegisterActivity"
            android:exported="false" /> <!-- Login Activity (No longer launcher) -->
        <activity
            android:name=".Login.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".AddDevice.AddDeviceActivity"
            android:exported="false" />
    </application>

</manifest>